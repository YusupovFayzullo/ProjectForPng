spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  datasource:
      driverClassName: org.postgresql.Driver
      username: postgres
      password: 123
      url: jdbc:postgresql://localhost:5432/filestorage

  profiles:
      active: prod


uploads:
   folder: /home/yusupov/uploads/


---
spring:
  config:
    activate:
      on-profile: prod
server:
  port: 9090